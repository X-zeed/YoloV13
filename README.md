# YOLOv13 Real-time Object Detection 🎥

โปรแกรมตรวจจับวัตถุแบบ Real-time ผ่านกล้อง Webcam ด้วย YOLO11 (YOLOv13)

![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)
![OpenCV](https://img.shields.io/badge/OpenCV-4.0+-green.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

## 📋 สารบัญ

- [คุณสมบัติ](#คุณสมบัติ)
- [ความต้องการของระบบ](#ความต้องการของระบบ)
- [การติดตั้ง](#การติดตั้ง)
- [วิธีใช้งาน](#วิธีใช้งาน)
- [การตั้งค่า](#การตั้งค่า)
- [แก้ปัญหา](#แก้ปัญหา)
- [FAQ](#faq)

## ✨ คุณสมบัติ

- ✅ ตรวจจับวัตถุแบบ real-time จากกล้อง webcam
- ✅ รองรับการตรวจจับวัตถุมากกว่า 80 ประเภท (คน, รถ, สัตว์, ฯลฯ)
- ✅ แสดง FPS และจำนวนวัตถุที่ตรวจพบ
- ✅ ปรับระดับความแม่นยำ (confidence threshold) ได้แบบ real-time
- ✅ วาด bounding boxes และ labels อัตโนมัติ
- ✅ รองรับโมเดล YOLO11 รุ่นล่าสุด

## 💻 ความต้องการของระบบ

### ซอฟต์แวร์
- Python 3.8 หรือสูงกว่า
- Webcam (กล้องในตัวหรือกล้อง USB)
- ระบบปฏิบัติการ: Windows, macOS, หรือ Linux

### ฮาร์ดแวร์แนะนำ
- CPU: Intel Core i5 หรือเทียบเท่า
- RAM: 4 GB ขึ้นไป
- GPU: ไม่จำเป็น (แต่จะช่วยเพิ่มความเร็วได้)

## 🚀 การติดตั้ง

### 1. ติดตั้ง Python

ดาวน์โหลดและติดตั้ง Python จาก [python.org](https://www.python.org/downloads/)

ตรวจสอบว่าติดตั้งสำเร็จ:
```bash
python --version
```

### 2. Clone หรือดาวน์โหลดโปรเจค

```bash
git clone https://github.com/yourusername/yolov13-detection.git
cd yolov13-detection
```

หรือดาวน์โหลดไฟล์ `yolo_webcam.py` แล้ววางในโฟลเดอร์ที่ต้องการ

### 3. ติดตั้ง Dependencies

```bash
pip install -r requirements.txt
```

หรือติดตั้งทีละตัว:
```bash
pip install ultralytics opencv-python numpy
```

### 4. (ทางเลือก) ติดตั้งสำหรับ GPU

ถ้ามี NVIDIA GPU และต้องการเร่งความเร็ว:
```bash
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```

## 📖 วิธีใช้งาน

### การใช้งานพื้นฐาน

```bash
python yolo_webcam.py
```

### ปุ่มควบคุมระหว่างใช้งาน

| ปุ่ม | หน้าที่ |
|------|---------|
| `q` | ออกจากโปรแกรม |
| `+` หรือ `=` | เพิ่ม confidence threshold |
| `-` | ลด confidence threshold |

### การใช้งานขั้นสูง

#### เลือกกล้องอื่น
```python
detector.run_webcam(camera_id=1)  # กล้องตัวที่ 2
```

#### ปรับ confidence threshold เริ่มต้น
```python
detector.run_webcam(conf_threshold=0.7)  # แม่นยำมากขึ้น
```

#### เลือกโมเดลอื่น
```python
detector = YOLOv13Detector(model_path='yolo11m.pt')  # โมเดลขนาดกลาง
```

## ⚙️ การตั้งค่า

### โมเดล YOLO ที่รองรับ

| โมเดล | ขนาด | ความเร็ว | ความแม่นยำ | แนะนำสำหรับ |
|-------|------|----------|------------|-------------|
| `yolo11n.pt` | ~5 MB | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | Real-time, คอมสเปคต่ำ |
| `yolo11s.pt` | ~10 MB | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | สมดุลระหว่างเร็วและแม่น |
| `yolo11m.pt` | ~25 MB | ⭐⭐⭐ | ⭐⭐⭐⭐ | ใช้งานทั่วไป |
| `yolo11l.pt` | ~50 MB | ⭐⭐ | ⭐⭐⭐⭐⭐ | ต้องการความแม่นยำสูง |
| `yolo11x.pt` | ~100 MB | ⭐ | ⭐⭐⭐⭐⭐ | แม่นที่สุด, มี GPU |

### ปรับความละเอียดกล้อง

แก้ไขในไฟล์ `yolo_webcam.py`:
```python
cap.set(cv2.CAP_PROP_FRAME_WIDTH, 1920)   # ความกว้าง
cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 1080)  # ความสูง
```

## 🔧 แก้ปัญหา

### ❌ กล้องไม่เปิด

**วิธีแก้:**
1. ตรวจสอบว่ากล้องทำงานปกติ (ลองเปิดแอพกล้องอื่น)
2. เปลี่ยน `camera_id` จาก 0 เป็น 1 หรือ 2
3. ตรวจสอบว่าไม่มีโปรแกรมอื่นใช้กล้องอยู่

### ❌ ModuleNotFoundError

**วิธีแก้:**
```bash
pip install --upgrade ultralytics opencv-python numpy
```

### ❌ โปรแกรมช้า / FPS ต่ำ

**วิธีแก้:**
1. ใช้โมเดลที่เล็กกว่า (yolo11n.pt)
2. ลดความละเอียดกล้อง
3. เพิ่ม confidence threshold (กรองวัตถุที่แน่ใจน้อย)
4. ปิดโปรแกรมอื่นที่กินทรัพยากร

### ❌ Permission denied (Linux/Mac)

**วิธีแก้:**
```bash
sudo chmod +x yolo_webcam.py
python3 yolo_webcam.py
```

### ❌ โมเดลดาวน์โหลดไม่สำเร็จ

**วิธีแก้:**
1. ตรวจสอบอินเทอร์เน็ต
2. ดาวน์โหลดโมเดลด้วยตนเอง:
```bash
wget https://github.com/ultralytics/assets/releases/download/v0.0.0/yolo11n.pt
```
3. วางไฟล์ในโฟลเดอร์เดียวกับโปรแกรม

## ❓ FAQ

### Q: โปรแกรมนี้ตรวจจับวัตถุอะไรได้บ้าง?

A: ตรวจจับได้มากกว่า 80 ประเภท รวมถึง: คน, รถยนต์, จักรยาน, สัตว์, เฟอร์นิเจอร์, อุปกรณ์อิเล็กทรอนิกส์, ฯลฯ ดูรายการเต็มได้ที่ [COCO dataset classes](https://tech.amikelive.com/node-718/what-object-categories-labels-are-in-coco-dataset/)

### Q: ต้องใช้อินเทอร์เน็ตตลอดเวลาไหม?

A: ต้องใช้อินเทอร์เน็ตเฉพาะครั้งแรกเพื่อดาวน์โหลดโมเดล หลังจากนั้นใช้งาน offline ได้

### Q: ทำงานบน Raspberry Pi ได้ไหม?

A: ได้ครับ! แนะนำใช้โมเดล yolo11n.pt และอาจต้องลดความละเอียดกล้อง

### Q: สามารถบันทึกวิดีโอได้ไหม?

A: ได้ครับ เพิ่มโค้ดนี้เข้าไป:
```python
fourcc = cv2.VideoWriter_fourcc(*'mp4v')
out = cv2.VideoWriter('output.mp4', fourcc, 20.0, (1280, 720))
# ใน loop
out.write(annotated_frame)
```

### Q: มี API สำหรับใช้งานไหม?

A: โค้ดนี้เป็น standalone application แต่สามารถดัดแปลงเป็น API ได้โดยใช้ Flask หรือ FastAPI

## 📝 รายการไฟล์

```
yolov13-detection/
│
├── yolo_webcam.py          # ไฟล์หลักของโปรแกรม
├── requirements.txt        # รายการ dependencies
├── README.md              # ไฟล์นี้
└── yolo11n.pt            # โมเดล YOLO (ดาวน์โหลดอัตโนมัติ)
```

## 📦 requirements.txt

```
ultralytics>=8.0.0
opencv-python>=4.5.0
numpy>=1.19.0
```

## 🤝 การมีส่วนร่วม

ยินดีรับ Pull Requests! สำหรับการเปลี่ยนแปลงใหญ่ กรุณาเปิด Issue ก่อน

## 📄 License

โปรเจคนี้อยู่ภายใต้ MIT License

## 👨‍💻 ผู้พัฒนา

- พัฒนาโดย: [Wachirawit Tanleng]
- Email: billwachi2550@gmail.com

## 🙏 เครดิต

- [Ultralytics YOLO](https://github.com/ultralytics/ultralytics) - YOLOv11 implementation
- [OpenCV](https://opencv.org/) - Computer vision library

## 📚 แหล่งข้อมูลเพิ่มเติม

- [Ultralytics Documentation](https://docs.ultralytics.com/)
- [YOLO Official Website](https://yolov5.com/)
- [OpenCV Python Tutorials](https://docs.opencv.org/master/d6/d00/tutorial_py_root.html)

---

⭐ ถ้าชอบโปรเจคนี้ อย่าลืมกด Star นะครับ!

**สร้างเมื่อ:** 2025  
**อัพเดทล่าสุด:** 30 พฤศจิกายน 2025
